# brain-tumor-cnn
A deep learning project that uses a Convolutional Neural Network (CNN) to classify brain MRI images into tumor and non-tumor cases. The model is trained on a Kaggle dataset and evaluated using standard classification metrics.
